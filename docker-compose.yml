version: "3.9"

services:
  well:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - well_data:/app/well_data
    restart: unless-stopped

volumes:
  well_data:
